import java.util.*;

/**
 * A class to use to learn about hashCode, equals, and hashMap
 */
public class USmap{
  /** The <code>HashMap</code> to experiment with */
  HashMap<City, State> states = new HashMap<City, State>();
  
  USmap(HashMap<City, State> states){
    this.states = states;
  }
  
  
}